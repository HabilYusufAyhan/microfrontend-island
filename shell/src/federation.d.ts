declare module "vueTodo/TodoApp" {
  const mount: (container: HTMLElement | null) => void;
  export default mount;
}

declare module "reactProfile/ProfileApp" {
  const mount: (container: HTMLElement | null) => void;
  export default mount;
}
